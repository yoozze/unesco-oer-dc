{#
/**
 * @file
 * Theme override for the node country field.
 */
#}
<div>
    <dt>{{ svg_icon('globe') }} <span>{{ label }}</span></dt>
    <dd>
        {% for item in items %}
            <span>{{ item.content }}</span>{% if not loop.last %}, {% endif %}
        {% endfor %}
    </dd>
</div>
