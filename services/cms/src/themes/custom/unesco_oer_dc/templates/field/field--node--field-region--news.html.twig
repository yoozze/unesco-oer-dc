{#
/**
 * @file
 * Theme override for the news node region field.
 */
#}
<div>
    <dt>{{ svg_icon('public') }} <span>{{ label }}</span></dt>
    <dd>
        {% if region_listing_links is defined and region_listing_links %}
            <ul class="c-article__meta-list">
                {% for link in region_listing_links %}
                    <li>
                        <a href="{{ link.url }}">{{ link.label }}</a>
                    </li>
                {% endfor %}
            </ul>
        {% endif %}
    </dd>
</div>
