{#
/**
 * @file
 * Theme override for the node region field.
 */
#}
<div>
    <dt>{{ svg_icon('public') }} <span>{{ label }}</span></dt>
    <dd>
        <ul class="c-article__meta-list">
            {% for item in items %}
                <li>{{ item.content }}</li>
            {% endfor %}
        </ul>
    </dd>
</div>
