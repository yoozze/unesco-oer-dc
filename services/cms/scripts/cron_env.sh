#! /bin/bash
# Placeholder overwritten at container start by write_cron_env.sh.
# Kept so the image has a file if entrypoint has not run yet.

export PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin
export COLUMNS=72
